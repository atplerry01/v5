namespace Whycespace.Domain.StructuralSystem.Cluster.Cluster;

public sealed class ClusterService
{
    // No domain service logic required for root cluster definition.
    // This domain defines structure only and contains no orchestration or execution logic.
}
