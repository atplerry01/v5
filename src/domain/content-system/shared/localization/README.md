# shared / localization

## Purpose

Groups the localization aggregate — cross-context locale variants for content (document translations, media dubbing variants, stream locale tracks).

## Leaf domains

- `localization/` — (SCAFFOLD) localization aggregate owning variant identity, source ref, locale, status.
