# shared / provenance-evidence

## Purpose

Groups the evidence aggregate — evidence linkage across content objects. Distinct from `document/integrity-provenance/provenance` (which is document-local custody); this is the cross-context evidence graph.

## Leaf domains

- `evidence/` — (SCAFFOLD) evidence aggregate owning evidence identity, bound targets, attestation chain, status.
