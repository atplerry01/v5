# shared / relationship

## Purpose

Groups the relationship aggregate — the cross-context content graph edge between any two content objects.

## Leaf domains

- `relationship/` — (SCAFFOLD) relationship aggregate owning edge identity, source ref, target ref, kind, status.
