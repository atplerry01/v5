namespace Whycespace.Domain.TrustSystem.Identity.Credential;

public sealed class CredentialService
{
    // Intentionally empty. Credential coordination logic is not yet required.
    // Reserved for future domain service operations that span multiple aggregates
    // or require orchestration beyond the CredentialAggregate boundary.
}
