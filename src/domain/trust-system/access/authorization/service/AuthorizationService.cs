namespace Whycespace.Domain.TrustSystem.Access.Authorization;

public sealed class AuthorizationService
{
    // Authorization structure domain service.
    // No access control evaluation logic — authorization decisions are
    // represented as aggregate state, not evaluated here.
}
