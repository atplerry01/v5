## New Guard Rule Capture

### CLASSIFICATION: domain / determinism
### SOURCE: Post-execution audit sweep — Phase 1 E2E Todo vertical slice
### DESCRIPTION: `AccountId.Generate()` in `src/domain/economic-system/capital/account/value-object/AccountId.cs` uses `Guid.NewGuid()` directly, violating deterministic execution rules (GE-01, DDIM-11.2, BDIM-24.2). This is pre-existing scaffold code, not introduced by this execution.
### PROPOSED_RULE: Scan all domain value objects for `Guid.NewGuid()`, `DateTime.Now`, `DateTime.UtcNow`, `Random` usage. All ID generation must go through `IIdGenerator` injected at runtime.
### SEVERITY: S0 — CRITICAL (blocks deterministic replay)
