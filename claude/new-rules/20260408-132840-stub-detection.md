# NEW RULE — stub-detection

CLASSIFICATION: structural / hygiene
SOURCE: phase1.5 structural integrity sweep (20260408-132840)
SEVERITY: S1/S2
STATUS: PROPOSED for promotion to canonical guards

## DESCRIPTION
Phase 1.5 sweep found 3 × S1 architectural placeholders that are tracked only by free-text comments (`PLACEHOLDER (T-PLACEHOLDER-01)`) — there is no canonical registry asserting that each placeholder has a planned replacement and a phase gate.

Current placeholders:
- T-PLACEHOLDER-01 — InMemoryWorkflowExecutionProjectionStore (src/platform/host/adapters/)
- T-PLACEHOLDER-02 — InMemoryStructureRegistry (src/runtime/topology/)
- WorkflowExecutionBootstrap.cs:100,102 — empty interface methods (intentional, but undocumented as no-op)

Also: TodoController.cs:87 has a silent `catch { }`.

## PROPOSED RULE
See `claude/guards/stub-detection.guard.md` (created in this sweep). Rules STUB-R1..R6 with CI enforcement.

## ACTION ITEMS
1. Promote `stub-detection.guard.md` into the CLAUDE.md guard list ($1a).
2. Create `claude/registry/placeholders.json` listing T-PLACEHOLDER-01 and T-PLACEHOLDER-02 with their replacement targets and phase gates.
3. Add architecture tests:
   - `NoNotImplementedExceptionInProductionPath`
   - `NoTodoFixmeHackOnProductionPath`
   - `EveryPlaceholderMarkerHasRegistryEntry`
   - `NoSilentCatchBlocks`
4. Narrow TodoController.cs:87 catch and add Debug log.
