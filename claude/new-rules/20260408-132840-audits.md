# NEW RULE — audits / tooling

CLASSIFICATION: tooling
SOURCE: phase1.5 structural integrity sweep (20260408-132840)
SEVERITY: S3
STATUS: PROPOSED

## DESCRIPTION
`scripts/dependency-check.sh` produces false positives that obscured the dep-graph audit:
1. Scans `/src/shared/obj/*.json` build artifacts for I/O leakage.
2. Flags `src/domain/business-system/integration/adapter/**` as "infrastructure adapters outside allowed paths" purely on filename match (`*Adapter*.cs`). These are domain aggregates modeling the *Adapter* concept in the Integration bounded context.

## PROPOSED RULE
1. `dependency-check.sh` MUST exclude `/obj/` and `/bin/` from all scans.
2. Adapter leakage check MUST scan for interface implementation markers (`: IEventStore`, `: IProjectionWriter`, `: IRepository<>`) instead of filename `*Adapter*.cs`.
3. Add unit test for the script itself with known-good and known-bad fixtures.

## ACTION ITEMS
- Refactor `scripts/dependency-check.sh` (defer to a separate prompt; not in scope for the Phase 1.5 fix pass).
