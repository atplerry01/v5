CLASSIFICATION: infrastructure
SOURCE: System validation — missing Todo topics in create-topics.sh
DESCRIPTION: The Kafka topic creation script (`infrastructure/event-fabric/kafka/create-topics.sh`) only defines topics for economic, identity, and operational.global.incident domains. The operational.sandbox.todo domain topics are missing. Additionally, outbox and chain migrations are not auto-applied during bootstrap (not in Postgres initdb.d mount).
PROPOSED_RULE: Every bounded context that produces events MUST have its Kafka topics declared in create-topics.sh. The bootstrap process MUST apply all migration sets (event-store, outbox, chain) to the event store database, not just the event-store migrations. Migration completeness must be validated as part of bootstrap verification.
SEVERITY: S2
