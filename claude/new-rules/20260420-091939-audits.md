---
CLASSIFICATION: DECISION-CLOSURE
SOURCE: P2.6.CS.6 — recording split inspection
DESCRIPTION: §DF-06 — recording event partitioning.
PROPOSED_RULE: All current Recording* events partition to the BROADCAST-side
  (archive). There are ZERO viewer-initiated events in the current aggregate,
  so `playback-consumption/replay` gains NO events from the split — it becomes
  a scaffold only (7 artifact folders + README, no aggregate yet).
SEVERITY: S2 (structural decision)
BLOCKING_FOR: P2.6.CS.6 (now unblocked)
STATUS: CLOSED.
---

## Event partition table

| Current event                | Verdict          | New home                          |
|------------------------------|------------------|-----------------------------------|
| RecordingStartedEvent        | broadcast-side   | live-streaming/archive            |
| RecordingCompletedEvent      | broadcast-side   | live-streaming/archive            |
| RecordingFailedEvent         | broadcast-side   | live-streaming/archive            |
| RecordingFinalizedEvent      | broadcast-side   | live-streaming/archive            |
| RecordingArchivedEvent       | broadcast-side   | live-streaming/archive            |

Rationale: no event carries viewer identity; no event represents a replay
request, replay progress, or replay termination. The aggregate tracks
`StreamRef` and `StreamSessionRef?` as capture SOURCE, not viewer
destination.

## CS.6 action

1. MOVE (not split): `streaming/persistence-and-observability/recording/`
   → `streaming/live-streaming/archive/`
2. Namespace: `Streaming.PersistenceAndObservability.Recording`
   → `Streaming.LiveStreaming.Archive`
3. Class / event / VO rename (Recording* → Archive*): DEFERRED to CS.13 per
   the CS.5 discipline capture (§claude/new-rules/20260420-091636-guards.md).
4. SCAFFOLD: create `streaming/playback-consumption/replay/` with 7 artifact
   subfolders + Band-S README marked SCAFFOLD. Zero migrated events.
5. Delete empty `streaming/persistence-and-observability/` group (recording
   was its only remaining leaf after CS.5 moved metrics out).

## Impact on plan

- CS.7 scaffold list was "4 missing streaming domains" after CS.4a reclassified
  availability as a move. `replay` is now scaffolded as part of CS.6 (not CS.7).
  CS.7 scope reduces to 3 scaffolds (entitlement-hook, moderation, progress,
  ingest-session — that's 4, not 3 — but availability was already removed, and
  replay is now in CS.6, so CS.7 = 4). Let me recount:

  Original 5 streaming missing (from plan):
    - availability           → MOVED via CS.5 (not scaffolded)
    - progress               → CS.7 scaffold
    - ingest-session         → CS.7 scaffold
    - entitlement-hook       → CS.7 scaffold
    - moderation             → CS.7 scaffold

  Target tree also had `replay` as a first-class missing domain under
  playback-consumption. That's handled in CS.6 (this gate) as a scaffold, not
  a split target.

  **Final CS.7 scope = 4 scaffolds: progress, ingest-session, entitlement-hook,
  moderation.** Unchanged from prior amended plan.
