# NEW RULE — config-safety

CLASSIFICATION: structural / configuration
SOURCE: phase1.5 structural integrity sweep (20260408-132840)
SEVERITY: S0/S1
STATUS: PROPOSED for promotion to canonical guards

## DESCRIPTION
Phase 1.5 sweep discovered systematic config-safety drift not previously covered by any canonical guard:
- 3 × S0 hardcoded credentials in `src/platform/host/appsettings.json`
- 5 × S1 hardcoded production endpoints (4 in appsettings, 2 in TodoBootstrap.cs, 1 in TodoController.cs)
- 8 × S2 magic timeouts/retries

The codebase already has a no-fallback pattern in `InfrastructureComposition.cs:30-40` and `ObservabilityComposition.cs:22-54`, but it is not enforced and `TodoBootstrap.cs:44,57` + `TodoController.cs:31-32` violate it.

## PROPOSED RULE
See `claude/guards/config-safety.guard.md` (created in this sweep). Rules CFG-R1..R5 with CI enforcement via architecture tests.

## ACTION ITEMS
1. Promote `config-safety.guard.md` into the CLAUDE.md guard list ($1a).
2. Add architecture tests in `tests/unit/architecture/WbsmArchitectureTests.cs`:
   - `AppsettingsJson_HasNoCredentials`
   - `AppsettingsJson_HasNoEndpointDefaults`
   - `NoFallbackInConnectionStringComposition`
3. Fix existing violations (tracked in fix pass of this sweep).
