CLASSIFICATION: infrastructure/kafka
SOURCE: System validation — KafkaOutboxPublisher runtime failure
DESCRIPTION: The outbox publisher uses a hardcoded default topic `whyce.events` that does not exist. Domain-aligned topic naming convention is `whyce.{classification}.{context}.{domain}.events` but the publisher does not route to domain-specific topics.
PROPOSED_RULE: Outbox publishers MUST route events to domain-aligned Kafka topics. No hardcoded default topic. The topic name must be derived from event metadata (classification, context, domain) matching the convention in create-topics.sh.
SEVERITY: S1
