# Phase 1.5 §5.1.1 — PASS Closure

## TITLE
Phase 1.5 §5.1.1 Dependency Graph Remediation — final closure (PASS).

## CLASSIFICATION
system / governance / dependency-control

## CONTEXT
Steps A–E and the H1/H3 final verification pass have all completed
successfully. This artifact records the canonical promotion of §5.1.1
from `IN PROGRESS` to `PASS` and freezes the closure evidence.

## OBJECTIVE
Documentation/status closure only. No structural code change. No guard
or script logic change. No reopening of the dependency model.

## CLOSURE NOTE
- **D1** — host → domain — CLOSED. Step C removed the sole typed usage in
  `src/platform/host/adapters/PostgresOutboxAdapter.cs` (replaced
  `AggregateId` pattern match with reflection on `.Value`) and removed
  `<ProjectReference Include="..\..\domain\Whycespace.Domain.csproj" />`
  from `src/platform/host/Whycespace.Host.csproj`.
- **D2** — host → engines — CLOSED and JUSTIFIED. Composition-root DI
  registration scope under DG-R5-EXCEPT-01.
- **D3** — host → runtime / systems / projections — CLOSED and JUSTIFIED.
  Composition-root DI registration and host adapter implementation scope
  under DG-R5-EXCEPT-01.
- **D4** — documentation/guard drift — CLOSED. Step D narrowed
  DG-R5-EXCEPT-01, added DG-R5-HOST-DOMAIN-FORBIDDEN (S0), rewrote the
  dependency-graph audit baseline to dated 2026-04-08, and updated
  README.md §5.1.1.
- **H1** — running Whycespace.Host process lock — CLOSED. PID 32648
  terminated; full host build succeeded with 0 warnings and 0 errors,
  transitively building all 8 projects.
- **H3** — C4 adapter-name over-match in `scripts/dependency-check.sh` —
  CLOSED. Single explicit whitelist clause `*/domain/*/adapter/*` added
  to the C4 case statement; no other C4 logic changed; no dependency
  guard rule weakened.

## EVIDENCE RECORDED
- **Clean build:** `dotnet build src/platform/host/Whycespace.Host.csproj
  -nologo -clp:NoSummary -v:m` → Build succeeded — 0 Warning(s),
  0 Error(s). Full 8-project dependency closure built (Shared, Domain,
  Engines, Runtime, Systems, Projections, Api, Host).
- **Script:** `bash scripts/dependency-check.sh` → `Violations: 0`,
  `Status: PASS`, `exit=0`.
- **Mechanical verifications:**
  - no `<ProjectReference>` to `Whycespace.Domain.csproj` in
    `Whycespace.Host.csproj`.
  - no `using Whycespace.Domain.*` in `src/platform/host/**/*.cs`.
  - no `using Whycespace.Domain.*` in `src/projections/**/*.cs`.
  - no `using Whyce.Runtime` in `src/projections/**/*.cs`.
  - C3 dependency graph has 0 cycles.

## ACCEPTANCE CRITERIA (workstream §7.0)
1. `src/projections` does not depend on `runtime` implementation. ✅
2. `platform/host` contains no forbidden dependency overreach. ✅
3. No forbidden dependency cycles exist. ✅
4. All remaining project references are canonically justified. ✅
5. Dependency graph audit passes. ✅
6. Guard and audit artifacts are updated. ✅
7. Build passes after remediation. ✅
8. Any exception is explicitly documented and governance-approved. ✅
   (DG-R5-EXCEPT-01, with DG-R5-HOST-DOMAIN-FORBIDDEN as the explicit
   counter-rule.)

## DOCUMENTS UPDATED IN THIS CLOSURE PASS
- [README.md §5.1.1](../../README.md) — status `IN PROGRESS` → `PASS
  (2026-04-08)`; closure note appended; final evidence summary appended.
- [README.md §6.0 master tracking table](../../README.md) — row 5.1.1
  status `NOT STARTED` → `PASS (2026-04-08)`.
- [claude/audits/dependency-graph.audit.md](../audits/dependency-graph.audit.md)
  — baseline summary block promoted to unconditional `PASS`, with
  inline workstream attribution and verification evidence.
- [claude/project-prompts/20260408-143500-phase-1-5-5-1-1-pass-closure.md](20260408-143500-phase-1-5-5-1-1-pass-closure.md)
  — this file.

## OUT OF SCOPE
- §5.1.2 Boundary Purity Validation.
- Future host-adapter extraction into a dedicated `src/infrastructure/**`
  project (Phase 2 candidate; recorded in DG-R5-EXCEPT-01 constraints).
- Any change to DG-R5-EXCEPT-01 or DG-R5-HOST-DOMAIN-FORBIDDEN.

## FINAL STATUS
**Phase 1.5 §5.1.1 Dependency Graph Remediation — PASS (2026-04-08).**
