# TITLE
Phase 2 Entry Determinism Certification Audit

# CONTEXT
Phase 1.5 is complete. Before entering Phase 2, the repo must pass a full determinism audit certifying that no product code violates the locked determinism rules ($9). This is a hardening and certification task, not a refactor.

# CLASSIFICATION
operational / infrastructure / determinism

# OBJECTIVE
Audit all source code and SQL for forbidden non-deterministic patterns (Guid.NewGuid, DateTime.UtcNow, DateTime.Now, DateTimeOffset.UtcNow, DateTimeOffset.Now, Random, NOW(), CURRENT_TIMESTAMP, gen_random_uuid, uuid_generate_v4). Classify each hit. Fix product violations. Document all accepted exceptions. Produce a formal certification report.

# CONSTRAINTS
- Do not modify architecture
- Do not replace SystemClock
- Do not rewrite test code unnecessarily
- Do not add domain dependencies to platform/host
- Prefer existing canonical seams (IClock, IIdGenerator)
- Minimal, precise edits only

# EXECUTION STEPS
1. Search src/** and infrastructure/** for all forbidden patterns
2. Classify each hit as: A (product violation), B (allowed infra/seam), C (test-only acceptable), D (needs governance)
3. Fix all A-classified items using existing canonical seams
4. Document B/C/D items with rationale
5. Re-run searches to verify product code is clean
6. Build to confirm zero compilation errors
7. Produce audit output, new-rule capture (if applicable), and this prompt record

# OUTPUT FORMAT
- Audit report: claude/audits/determinism-phase2-entry.audit.output.md
- New rule capture: claude/new-rules/20260413-120000-determinism-phase2-entry.md
- Prompt record: this file

# VALIDATION CRITERIA
- Zero product-code determinism violations remain
- All remaining hits justified and documented
- Build compiles with 0 errors
- Audit evidence file written
- Doctrine clarification captured if needed

# RESULT
**PASS** — Executed 2026-04-13. One product violation found and fixed (ApiResponse.cs DateTime.UtcNow). All other hits classified as allowed infrastructure/documentation. SQL NOW() governance rule clarified. Build verified clean.
