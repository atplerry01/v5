# WBSM v3.5 — HSID v2.1 (BUCKETED SEQUENCE) FULL PATCH

CLASSIFICATION: core-system / determinism / identity
TYPE: IMPLEMENTATION + GUARD + AUDIT
MODE: STRICT / NO-DRIFT / ENFORCED
STORED: 2026-04-07 19:30:00

## OBJECTIVE
Upgrade deterministic identity system to HSID v2.1:
1. Bucketed sequence (bounded, scalable)
2. Topology-aware IDs (Cluster → SubCluster → SPV)
3. Mandatory runtime enforcement (ALL requests)
4. Guard + audit + CI enforcement

## CANONICAL ID FORMAT
`PPP-LLLL-TTT-CCCXXXSSSSSS-RRR`
Example: `EVT-UKED-1A3F-A91FBC88D2AB-00F4`

Segments: PPP=Prefix, LLLL=Location, TTT=TimeBucket, CCC=Cluster,
XXX=SubCluster, SSSSSS=SPV, RRR=rolling sequence (LAST).

## EXECUTION STEPS

1. Shared contract
   - CREATE src/shared/kernel/determinism/IDeterministicIdEngine.cs
   - CREATE src/shared/kernel/determinism/TopologyCode.cs
   - CREATE src/shared/kernel/determinism/TimeBucket.cs

2. T0U engine
   - CREATE src/engines/t0u/determinism/DeterministicIdEngine.cs

3. Time bucket provider
   - CREATE src/engines/t0u/determinism/time/ITimeBucketProvider.cs
   - CREATE src/engines/t0u/determinism/time/DeterministicTimeBucketProvider.cs

4. Sequence resolver
   - CREATE src/engines/t0u/determinism/sequence/ISequenceResolver.cs

5. Runtime enforcement
   - MODIFY src/runtime/control-plane/RuntimeControlPlane.cs
     - Validate CommandId present
     - Validate via IDeterministicIdEngine.IsValid
     - Auto-generate via Generate(...) if missing

6. Guard
   - CREATE claude/guards/deterministic-id.guard.md (G1..G7)

7. Audit
   - CREATE claude/audits/deterministic-id.audit.md (A1..A5)

8. CI
   - CREATE scripts/deterministic-id-check.sh

## CONSTRAINTS
- $5 anti-drift: no renaming, no pattern inference
- $7 layer purity: domain MUST NOT generate IDs
- $9 determinism: no Guid.NewGuid, IClock only
- $8 policy: all via WHYCEPOLICY
- IdPrefix + LocationCode types are referenced but not defined in patch — REQUIRES CLARIFICATION before CREATE

## VALIDATION CRITERIA
- 0 Guid.NewGuid in src/
- 100% HSID v2.1 format compliance
- Runtime rejects missing/invalid CommandId
- Guard + audit + CI script present

(See original request in conversation for full code blocks.)
