# R3.B.5 Compensation Emission — Post-Execution Audit Sweep

**Prompt:** [20260420-140127-runtime-r3-b-5-compensation.md](../../project-prompts/20260420-140127-runtime-r3-b-5-compensation.md)
**Builds on:** R3.B.1/2/3/4 sweeps.
**Sweep date:** 2026-04-20 14:15 UTC

---

## 1. Guard Validation ($1a)

Guards loaded fresh. Three new rules promoted under new section "R3.B.5 Compensation Emission":
- `R-OUT-EFF-COMPENSATION-EMIT-01` — canonical problem outcomes that require emission.
- `R-OUT-EFF-COMPENSATION-ATOMIC-01` — same-fabric-batch atomicity.
- `R-OUT-EFF-COMPENSATION-UNHANDLED-01` — loud evidence on orphan handlers.

## 2. Build Status

- All production assemblies clean.
- Integration + unit test projects clean.

## 3. Test Status

### New tests shipped with R3.B.5
| Test | Covers |
|---|---|
| `Policy_requires_compensation_matches_design` (10 Theory cases) | Shape × outcome matrix |
| `Finalized_BusinessFailed_emits_compensation_atomically_with_finalized` | Emission from finalize path |
| `Reconciled_BusinessFailed_emits_compensation_atomically_with_reconciled` | Emission from reconcile path |
| `Reconciled_PartiallyCompleted_emits_compensation_for_AtMostOnce_shape` | Shape gate on PartiallyCompleted |
| `Reconciled_PartiallyCompleted_does_NOT_emit_compensation_for_ProviderIdempotent_shape` | Shape gate negative case |
| `Replay_does_not_duplicate_compensation_emission` | Replay safety |
| `Repeated_finalize_refuses_and_does_not_duplicate_compensation` | Per-trigger idempotency via terminal-state precondition |
| `Example_handler_receives_signal_and_observes_caller_workflow_trigger` | Caller-consumption pattern proof |
| `Missing_handler_still_emits_compensation_event_and_alerts` | Orphan path emits durable event |

### R3.B.3 tests repaired for R3.B.5 behavior
- `OutboundEffectShapeMatrixTests.Classification_shape_matrix_lands_on_expected_routing` — Terminal-classification cases now assert `CompensationRequested` status + both `RetryExhausted` + `CompensationRequested` events (reflects the R3.B.5 atomic-batch change).
- `AtMostOnce_Terminal_exhausts_without_reconciliation` — same update; still asserts no reconciliation path taken.

### Cumulative R3.B totals
- Integration outbound-effect: **69 tests GREEN**.
- Unit outbound-effect: **27 tests GREEN**.
- **96 total R3.B tests GREEN across R3.B.1+2+3+4+5.**

## 4. Dependency Graph

No new forbidden edges:
- `OutboundEffectCompensationDispatcher` lives in runtime; references shared contracts only — R11 clean.
- `IOutboundEffectCompensationHandler` + `OutboundEffectCompensationSignal` live in shared contracts; shared does not reference domain (the DTO pattern avoids the domain-layer leak).
- Finality service + relay gained optional `OutboundEffectCompensationDispatcher?` constructor param — backward compatible with R3.B.4 test setups.

## 5. Architectural Invariants Verified

- **R-OUT-EFF-COMPENSATION-EMIT-01** — `OutboundEffectCompensationPolicy.RequiresCompensation` theory test pins the (outcome × shape) matrix; finality service emits on every required case, does NOT emit on forbidden cases.
- **R-OUT-EFF-COMPENSATION-ATOMIC-01** — both trigger sites (finality service + relay) build a single event list and pass it to `IEventFabric.ProcessAsync` in one call; atomicity is structural.
- **R-OUT-EFF-COMPENSATION-UNHANDLED-01** — `Missing_handler_still_emits_compensation_event_and_alerts` pins the discipline: event on stream + metric + log, not silent drop.
- **Replay safety** — `Replay_does_not_duplicate_compensation_emission` confirms the aggregate's `Apply` reconstructs `CompensationRequested` status idempotently on repeated replay.
- **Per-trigger idempotency** — `Repeated_finalize_refuses_and_does_not_duplicate_compensation` proves the finalize-precondition (terminal-state refuse) prevents re-emission across repeated caller invocations.
- **R-OUT-EFF-DET-01** — no new entropy sources in compensation-code subtree.

## 6. Drift Rules Captured ($1c)

None. R3.B.5 stays within the R3.B ratified design envelope. The only behavior change that rippled into prior tests (Terminal classification now ending in `CompensationRequested` instead of `RetryExhausted`) is a direct consequence of the ratified atomic-batch emission rule — the prior tests were pinned to pre-R3.B.5 state and were updated in lock-step with the new guard.

## 7. Anti-Drift ($5)

- No new middleware stages.
- No new lifecycle events beyond the eleven R3.B.1 canonical set — `OutboundEffectCompensationRequestedEvent` shipped in R3.B.1; R3.B.5 only adds emission path + consumer wiring.
- No policy changes to the dispatch / finality / reconciliation paths.
- Finality service + relay signatures additive only (new optional ctor param).

## 8. Sweep Result

**PASS.** R3.B.5 lands with:
- 18 new tests passing (10-Theory policy matrix + 8 emission/replay/handler tests) + 4 repaired shape-matrix assertions.
- 3 new guard rules authored.
- Compensation emission operative from all four canonical problem outcomes.
- Example caller-handler pattern proven (test adapter receives signals and observes triggering outcome).
- Missing-handler path emits durable event + metric + log.

## 9. Next Checkpoint

**§15 External Side-Effect Control is now materially complete.** Remaining §15 row "Side-effect observability" stays PARTIAL only for the dashboards + alert-rule package (R4.A). No runtime-correctness work remains on the outbound-effect subsystem.

R3 series complete. Next sequence per user roadmap: R4.B (operator controls) → R4.A (observability package) → R4.C (security hardening) → R5 (replay-equivalence / chaos / soak / certification).
