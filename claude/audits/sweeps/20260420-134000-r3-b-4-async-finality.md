# R3.B.4 Async Finality + Reconciliation — Post-Execution Audit Sweep

**Prompt:** [20260420-132955-runtime-r3-b-4-async-finality.md](../../project-prompts/20260420-132955-runtime-r3-b-4-async-finality.md)
**Builds on:** R3.B.1/2/3 sweeps.
**Sweep date:** 2026-04-20 13:40 UTC

---

## 1. Guard Validation ($1a)

Guards loaded fresh. Four new rules promoted under new section "R3.B.4 Async Finality + Reconciliation":
- `R-OUT-EFF-FINALITY-COMMAND-01` — finality mutations flow through `IOutboundEffectFinalityService`.
- `R-OUT-EFF-ORPHAN-CALLBACK-01` — orphan callbacks emit deterministic evidence, never silently drop.
- `R-OUT-EFF-RECONCILE-PRECONDITION-01` — reconcile only from `ReconciliationRequired`.
- `R-OUT-EFF-TIMEOUT-SWEEP-01` — ack/finality timeouts emit canonical lifecycle events.

## 2. Build Status

- Host / Runtime / Shared / Projections / Engines — clean, 0 warnings.
- Integration tests — clean.
- Unit tests — clean (pre-existing CS0162 unchanged).

## 3. Test Status

### New tests shipped with R3.B.4
| File | Count | Covers |
|---|---|---|
| `OutboundEffectFinalityServiceTests.cs` | 8 | Finalize drops deadlines; Reconcile precondition; Acknowledged≠Finalized non-conflation; ack-timeout sweep; finality-timeout sweep; poll-Succeeded finalizes; poll-Unresolvable reconciles |
| `WebhookCallbackIngressHandlerTests.cs` | 6 | Happy-path finalizes; orphan effect id; signature mismatch; unknown outcome code → reconciliation; reconciliation-required routes to reconcile; finalized status rejects late callback |

### R3.B cumulative totals
- Integration outbound-effect suite: **51 tests GREEN** (14 R3.B.1/2 + 23 R3.B.3 + 14 R3.B.4).
- Unit outbound-effect suite: **27 tests GREEN** (unchanged).
- **78 total R3.B tests GREEN.**

## 4. Dependency Graph

No new forbidden edges:
- `WebhookCallbackIngressHandler` lives in `src/runtime/outbound-effects/` with no direct HTTP invocation — the signature verifier consumes `byte[] body` supplied by the caller. The actual ASP.NET endpoint wiring is a thin controller that calls this handler (deferred to R4.B admin surface).
- `OutboundEffectReconciliationSweeper` in `src/runtime/outbound-effects/` consumes the existing queue store + adapter registry + finality service — no new cross-layer imports.
- `OutboundEffectReconciliationSweeperWorker` under `src/platform/host/adapters/outbound-effects/` matches the relay-worker precedent.

## 5. Architectural Invariants Verified

- **R-OUT-EFF-FINALITY-COMMAND-01** — finality transitions routed through `IOutboundEffectFinalityService` with factory-constructed events; direct event construction outside the factory remains forbidden.
- **R-OUT-EFF-ORPHAN-CALLBACK-01** — `WebhookCallbackIngressHandlerTests.Orphan_effect_id_rejects_with_unknown_effect_outcome` + `Signature_mismatch_rejects_deterministically` prove orphan/invalid callbacks translate to deterministic outcome codes; `Unknown_outcome_code_emits_reconciliation_required` pins the "correlated but unrecognized outcome → reconciliation" contract.
- **R-OUT-EFF-RECONCILE-PRECONDITION-01** — `Reconcile_refuses_when_status_is_not_reconciliation_required` proves the strict precondition.
- **R-OUT-EFF-TIMEOUT-SWEEP-01** — `Ack_timeout_triggers_reconciliation_required_via_sweeper` + `Finality_timeout_on_manual_only_adapter_triggers_reconciliation_required` prove ack/finality timeout → explicit lifecycle event, not logs. Poll-Succeeded / Poll-Unresolvable tests prove the `Poll`/`Hybrid` branches.
- **Non-conflation (ratified constraint #1)** — `Acknowledged_and_Finalized_remain_non_conflated_after_finalize` asserts both events exist on the stream in correct order after `FinalizeAsync`.
- **R-OUT-EFF-DET-01** — no new entropy sources in finality/sweeper/ingress code (consistent with the R3.B.1 subtree scan).

## 6. Drift Rules Captured ($1c)

None. R3.B.4 stayed within the ratified R3.B design envelope. `IOutboundEffectAdapter.PollFinalityAsync` was added as a default interface method throwing `NotSupportedException` for non-polling adapters — backward-compatible, matches design §8.2.

## 7. Anti-Drift ($5)

- No new middleware stages.
- No new lifecycle events beyond the eleven R3.B.1 canonical set.
- No policy-evaluation changes.
- `IOutboundEffectQueueStore` extended additively (new `ClaimExpiredOrPollDueAsync` method); in-memory + Postgres implementations updated in lock-step.
- Projection read model extended additively (new fields default-valued).

## 8. Sweep Result

**PASS.** R3.B.4 lands with:
- 14 new tests passing.
- 4 new guard rules authored.
- Finality service, reconciliation sweeper, webhook ingress handler, poll-based finality, extended projection read model, and extended queue store all in place.
- Async finality path is real: webhook ingress → correlated Finalized event; ack/finality timeouts → canonical reconciliation events; poll path operative for `Poll`/`Hybrid` adapters.

## 9. Next Checkpoint

- R3.B.5 — automatic `OutboundEffectCompensationRequestedEvent` emission from `Finalized(BusinessFailed)` / `RetryExhausted` / `Reconciled(BusinessFailed | PartiallyCompleted)`; example compensation-workflow integration handler wired against a caller aggregate; §15 S1 rows move to materially complete closure.
