# R3.B.3 Outbound-Effect Hardening — Post-Execution Audit Sweep

**Prompt:** [20260420-131317-runtime-r3-b-3-hardening.md](../../project-prompts/20260420-131317-runtime-r3-b-3-hardening.md)
**Builds on:** R3.B.1 sweep 20260420-124147 + R3.B.2 sweep 20260420-130333.
**Sweep date:** 2026-04-20 13:23 UTC

---

## 1. Guard Validation ($1a)

Guards loaded fresh. Three new rules promoted into `runtime.guard.md`
under a new "R3.B.3 Outbound-Effect Hardening" section:
`R-OUT-EFF-AMBIGUOUS-ATMOSTONCE-01`, `R-OUT-EFF-BACKOFF-DET-01`,
`R-OUT-EFF-QUEUE-PAYLOAD-REGISTRY-01`. No structural changes to the
4-layer canonical guard model.

## 2. Build Status

- Domain / Engines / Runtime / Projections / Host — clean, 0 warnings.
- Integration tests — clean, 0 warnings (prior SYSLIB0050 eliminated by switching to `RuntimeHelpers.GetUninitializedObject`).
- Unit tests — clean (pre-existing CS0162 unchanged; unrelated to R3.B).

## 3. Test Status

### New tests shipped with R3.B.3
| File | Count | Covers |
|---|---|---|
| `OutboundEffectShapeMatrixTests.cs` | 9 (1 Theory × 8 cases + 1 Fact) | Per-shape classification routing (`R-OUT-EFF-AMBIGUOUS-ATMOSTONCE-01`) |
| `OutboundEffectBackoffDeterminismTests.cs` | 5 | Replay-determinism of `ComputeBackoffMs`; seed encoding; Retry-After precedence; cap enforcement |
| `OutboundEffectAggregateReplayEquivalenceTests.cs` | 5 (1 Theory × 5 streams) | Two replays land identical status across ack / retry / exhaust / reconciliation / cancel paths |
| `PostgresOutboundEffectQueueStorePayloadRegistryTests.cs` | 4 | Registered round-trip; fail-closed on unregistered type (serialize); fail-closed on unknown name (deserialize); envelope carries registry short name, not AQN |
| `OutboundEffectProhibitionTests.Relay_Backoff_Helper_Is_Replay_Deterministic` | 1 | Architecture scan: no clock/random/entropy in `ComputeBackoffMs` body |

### Combined R3.B totals
- Integration outbound-effect suite: **37 tests GREEN**.
- Unit outbound-effect suite: **27 tests GREEN**.
- **64 total R3.B tests GREEN across R3.B.1 + R3.B.2 + R3.B.3.**
- No regressions in any previously-green test.

## 4. Dependency Graph

No new forbidden edges:
- `OutboundEffectRelay.ComputeBackoffMs` promoted to `public static` — same file, no new imports.
- `PostgresOutboundEffectQueueStore` gained `IPayloadTypeRegistry` ctor dependency from `Whycespace.Shared.Contracts.EventFabric` — shared-contracts, valid for the platform/host/adapters layer.
- No new assembly references.

## 5. Architectural Invariants Verified

- **R-OUT-EFF-AMBIGUOUS-ATMOSTONCE-01** — `OutboundEffectShapeMatrixTests` covers the 2×4 = 8 cell matrix plus the dual check that Terminal-on-AtMostOnce still exhausts (does NOT over-fire the reconciliation path).
- **R-OUT-EFF-BACKOFF-DET-01** — unit arch test `Relay_Backoff_Helper_Is_Replay_Deterministic` scans the method body for forbidden entropy; 5 integration tests pin the computed-value determinism and seed format.
- **R-OUT-EFF-QUEUE-PAYLOAD-REGISTRY-01** — 4 tests prove round-trip + fail-closed on unknown types (both sides) + registry-short-name envelope format.
- **R-OUT-EFF-IDEM-05** (from R3.B.1) — now materially proven end-to-end: the relay enforces it via `IsAmbiguousUnsafeToRetry` + shape-matrix test coverage.
- **R-OUT-EFF-DET-01** (from R3.B.1) — still green; the new `public static` helper stays inside the outbound-effect subtree that the determinism architecture test scans.

## 6. Drift Rules Captured ($1c)

None. All R3.B.3 work stayed within the R3.B ratified design envelope. The `ComputeBackoffMs` promotion from private instance method to `public static` is a pure refactor that preserves behavior and does not amend any design decision.

## 7. Anti-Drift ($5)

- No new middleware stages.
- Relay signature backward-compatible (added optional `ICircuitBreakerRegistry?` in R3.B.2; R3.B.3 adds no new ctor params).
- No lifecycle event additions or removals — the eleven R3.B.1 events remain the full canonical set.
- Postgres queue store ctor gained a REQUIRED `IPayloadTypeRegistry`; the composition bootstrap was updated to inject it in the same checkpoint.

## 8. Sweep Result

**PASS.** R3.B.3 lands with:
- 24 new tests passing (23 integration + 1 unit architecture).
- 3 new guard rules authored and matched by architecture tests.
- Correctness bug closed: Ambiguous-on-AtMostOnce no longer exhausts silently.
- Payload deserialization hardened: unknown types now fail loudly, not silently.
- Backoff formula pinned as replay-deterministic via architecture test — foundation for R5 replay-equivalence certification.

## 9. Next Checkpoints

- R3.B.4 — webhook ingress + scheduled finality poller + reconciliation-required automatic emission + orphan-callback handler + reconcile-command operator surface.
- R3.B.5 — compensation emission from `Finalized(BusinessFailed)` / `RetryExhausted` / `Reconciled(BusinessFailed | PartiallyCompleted)` + example compensation workflow wiring.
- R4.B — admin/operator controls (pause / re-drive / DLQ-inspect / outbound-effect inspect and reconcile).
