# phase1.5 S0-2 (CFG-R1): credentials moved out of compose files.
# Copy to `.env` (gitignored) and set real values for any non-local
# deployment. Local dev defaults below are NOT secrets — rotate before
# any shared / network-exposed environment.

POSTGRES_USER=whyce
POSTGRES_PASSWORD=change_me_securely

MINIO_ROOT_USER=whyce
MINIO_ROOT_PASSWORD=change_me_securely
